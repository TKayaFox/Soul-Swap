//Check if the player is within attackable range, if found try to attack them
function Try_Attack(attackCoolDown, attackRange, target){
	//Ensure target is valid
	if (instance_exists(target))
	{
		
	}
	
	//reset attack cooldown
}