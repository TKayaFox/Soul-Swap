/// @description Possess Body on [Enter] held if able

if(player_possessed)
{
	//Make sure key is held for possess_hold_delay duration first
	
	//Check for vessels within vessel_range
	
	//Check for nearest_vessel (If anything is close enough to possess
	if (!nearest_vessel==null)
	{
		//Set the new vessel as possessed
		
		//Destroy ghost
		//destroy_instance();
	}
}